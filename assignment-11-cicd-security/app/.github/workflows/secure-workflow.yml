name: Secure CI Pipeline

on:
  push:
    branches:
      - assignment-11-cicd-security
  pull_request:
    branches:
      - assignment-11-cicd-security

permissions:
  contents: read

jobs:
  security-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
